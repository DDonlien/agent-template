# 20260817-103337-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

移除一二级标题的数字编号，避免 Agent 错误地认为他们应该按顺序执行。他们应该模块化地按照任务本身要求去执行。

每次任务开始前的部分，增加一条规则，检查 source 文件里的 template 是否有更新？如果有的话，把通用部分更新下来后再阅读新的 agents 文件、再执行用户的任务。具体的更新规则参考 agent template 更新规则部分。其中内容也按照我们这个要求去做一些实际的调整。

Walk tree 的部分：我做了手动更新，同步到另一个文档里。

## 启动运行时的分支和版本

1. 分支：main。
2. 版本：62125fc。
3. 同步：已执行 `git fetch --prune`；upstream 为 origin/main，ahead / behind 为 0 / 0，未执行 merge。

## 任务执行时间

1. 开始：2026-08-17 10:27:29 CST。
2. 结束：2026-08-17 10:33:37 CST。
3. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 AGENTS.md。
2. 两份当前子模板 AGENTS.md。
3. 当前模板的 source 元数据和 origin/main 中对应模板文件，用于检查 source 是否有更新。
4. `game-agent-template/universe/AGENTS.md`，用于理解其作为 Game 子规则的边界。
5. 工作区现有执行日志及上一轮结构重组结果。

## 对话与行动记录

1. 检查发现 App 模板的 Worktree 树形结构已有用户手动更新，Game 模板仍是旧树形结构。
2. 将 App 模板的 Worktree 树形结构同步到 Game 模板，并移除该手动更新中的尾随空格。
3. 移除两份子模板所有一级、二级标题的数字前缀，并将正文中的旧章节编号引用改为模块名称，避免标题顺序被误解为执行顺序。
4. 在 Agent Template 更新规则中增加 source 检查、通用规则同步、冲突保护、版本同步和 source 不可访问时的风险记录规则。
5. 在每次任务开始前增加 source 更新检查流程，要求先同步通用规则、再重新阅读更新后的 AGENTS.md 和相关文档、最后执行用户任务。
6. 更新顶层 AGENTS.md 的标准内容说明、复制清理引用和版本号。
7. 执行 source 预检：origin/main 与本地基线一致，远端模板版本早于本次未提交的本地模板，无需同步远端变更。

## 完成工作

1. 更新 app-agent-template/AGENTS.md，版本更新为 v1.4.0-260817-103337。
2. 更新 game-agent-template/AGENTS.md，版本更新为 v1.5.0-260817-103337。
3. 更新顶层 AGENTS.md，版本更新为 v1.0.2-260817-103337。
4. 新增本次执行日志；保留工作区中此前已有的其他未跟踪日志。

## 更新的需求 ID

1. 无新增稳定需求 ID；本次属于顶层 meta-DOC-A-001 元模板结构和同步规则维护。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. 确认两份子模板各有 3 个一级标题、23 个二级标题，且一级和二级标题均无数字前缀。
2. 确认两份子模板不存在实际三级标题语法。
3. 确认每个二级标题下的非代码内容均为有序列表，嵌套清单按 TAB 缩进。
4. 确认两份模板的通用规则内容完全一致。
5. 确认两份模板的 Worktree 章节完全一致，并包含用户手动更新的树形结构。
6. 确认四项元数据、GitHub source 链接和 Game Godot Headless 条目仍然存在。
7. 确认顶层 AGENTS.md 不再引用已移除的子模板章节编号。
8. `git diff --check` 通过。

## 备注

1. 本次修改两份子模板 AGENTS.md、顶层 AGENTS.md 和顶层执行日志，未修改 README、REQUIREMENTS、DESIGN 或 universe 子模板。
2. 未执行 App、Game、Godot 或 UE 运行时测试，本次交付物是文档结构和协作规则调整。
