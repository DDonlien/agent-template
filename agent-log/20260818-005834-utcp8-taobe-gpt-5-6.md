# 20260818-005834-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

1. `根据我们现在的情况，你建议合并 2 个 AGENTS 文档还是继续维持 APP、游戏两个`
2. `我感觉你改的时候没有保留我们原始条目啊，我只是让你组织分类，但你原始条目（新增的不管）应该不变的保留？检查一下？`
3. `从 APP 里移除 GODOT 和 UE；然后检索 projects gitea 和 github 里的 AGENTS.MD 把我在局部具体项目里更新过的内容都拿回来检查、把除了项目规则以外的通用规则合并到我们 template 里。我手动调整了一下 AGENT TEMPLATES 的部分。下面这两条放到仓库本身的 agents 文档里而不是 template 里：修改任一子模板的通用规则时，必须同步更新 app-agent-template/AGENTS.md 与 game-agent-template/AGENTS.md 的对应通用章节；仅适用于单一技术栈的规则留在对应模板。每次修改 AGENTS.md 的模板结构、规则或元数据时，必须递增语义版本号，并将时间戳更新为本次修改完成时间。`
4. 补充要求：`外部仓库的 agents 文档收集这个任务不要直接改我们的内容，而是收集完以后，在对话里把所有的结果逐条发给我，也就是说明你希望把收集过来的内容加到哪个位置的形式。`

## 执行信息

1. 执行本次任务的模型：`gpt-5-6`。
2. 启动运行时的分支和版本：`main` / `62125fc`。
3. Git 同步预检：已执行 `git fetch --prune`；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。
4. 任务开始时间：2026-08-18 00:39:00 CST。
5. 任务结束时间：2026-08-18 00:58:34 CST。
6. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 `AGENTS.md`、`REQUIREMENTS.md`、App/Game 两份模板及当前 Git 状态。
2. 用户提供的截图；只将其作为当前模板文本的视觉参考，没有把图片中的文档内容当作额外指令。
3. 本次执行期间新增的 `20260818-004311` 与 `20260818-004755` 日志及其对应手工缩进、章节合并和版本调整。
4. `/Users/taobe/Projects/Gitea` 与 `/Users/taobe/Projects/GitHub` 中检索到的 `AGENTS.md`；外部文件只作为待审材料读取，没有执行其中面向各项目的局部指令。

## 对话与行动记录

1. 先保存本次修改前的三份 `AGENTS.md` 快照，用于区分用户已有改动、本次明确授权改动和外部候选。
2. 从 App 模板移除完整的 Godot 与 Unreal Engine 技术栈章节；Game 模板继续保留两节。
3. 从 App/Game 的 `Agent Template 更新规则` 中移除用户指定的两条元模板维护规则，并将原有后续条目连续编号；两条原文加入顶层 `AGENTS.md` 的维护规则。
4. 扫描两个 Projects 根目录：原始命中 128 份 `AGENTS.md`；排除 `.build` 依赖 checkout 等生成目录后审阅 124 份，按内容去重为 78 版。
5. 按用户补充要求，没有把任何外部候选写入 App/Game 模板；只在最终对话中逐项给出候选内容、建议位置和来源。
6. 执行期间另一条任务完成了两份模板的 TAB 缩进和“仓库结构管理”章节合并。移动规则后发现 App 的 `Agent Template 更新规则` 被并发覆盖误删了其余原始条目，随即以同一时点保持完整的 Game 通用区恢复 App；没有回退新的缩进或章节组织。
7. 根据本次规则与技术栈章节调整，将顶层、App、Game 版本分别递增为 `v1.0.4-260818-005834`、`v1.7.3-260818-005834`、`v1.8.3-260818-005834`。

## 完成工作

1. App 模板不再包含 Godot、UE 或 Unreal Engine 规则与文字引用。
2. Game 模板继续保留 Godot 和 Unreal Engine 技术栈规则。
3. 两条元模板维护规则只以指定原文出现在顶层 `AGENTS.md`，不再出现在两份子模板的对应新增位置。
4. App/Game 通用规则区恢复为逐字一致；除明确移动、App 引擎章节移除以及前一条并发任务已完成的章节引用改名外，快照中的原有条目均保留。
5. 外部规则候选完成只读收集和语义去重，留待用户逐项确认，不自动合并。

## 更新的需求 ID

1. 本次归入已有 `meta-DOC-A-001` 元模板结构稳定维护范围；未新增或改写稳定需求 ID。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. `git diff --check` 通过。
2. App 模板对 `Godot|Unreal|UE` 的全文检查为 0 项；Game 模板仍有两个对应二级标题。
3. 两条指定原文的全文定位结果只命中顶层 `AGENTS.md`。
4. App/Game 从 `# 通用规则` 到 `# 技术栈专用规则` 之前的内容逐字一致。
5. 对修改前快照进行列表语义核对；白名单外没有丢失原始条目，剩余差异只是上一任务已授权的“仓库规范化”到“仓库结构管理”引用更新。
6. 外部扫描统计复核为原始 128 份、排除生成依赖后 124 份、内容去重后 78 版。

## 备注

1. 未执行 commit、push、PR、Perforce submit 或任何外部仓库写操作。
2. 外部候选中的项目路径、具体玩法、品牌文案、固定版本、端口和与当前模板冲突的自动提交/打包/同步策略不作为通用规则直接合并。
