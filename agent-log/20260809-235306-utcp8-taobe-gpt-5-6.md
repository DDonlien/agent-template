# 20260809-235306-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

```text
给 GAME TEMPLATE 里增加一个规则，GODOT 专用，除非无法实现或者用户要求，否则用 GODOT HEADLESS 做测试
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`1dc688b2b08e636439e5815294e6fd866fae1aa8`
- 同步：已执行 `git fetch --prune`；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。

## 任务执行时间

- 开始：`2026-08-09 23:52:34 CST`
- 结束：`2026-08-09 23:53:06 CST`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- 顶层 `AGENTS.md`（元模板维护规范）
- `game-agent-template/AGENTS.md`（编辑对象）
- `game-agent-template/README.md`
- `game-agent-template/REQUIREMENTS.md`
- 最近的顶层执行日志 `agent-log/20260807-214301-utcp8-taobe-deepseek-v4-flash.md` 及此前两条相关日志

## 匹配到的需求

- 无新增稳定需求 ID；本次属于元模板协作规范维护，与顶层 `REQUIREMENTS.md` 的 `meta-DOC-A-001`（保持 Game 子模板可用）一致。

## 对话与行动记录

- 检查工作区、分支、upstream 与远端同步状态，确认工作区原先干净且当前分支可继续。
- 按用户明确的 Game 模板、Godot 专用范围，在 `game-agent-template/AGENTS.md` 新增 Godot 引擎项目专门要求。
- 将规则写明为：除非 Headless 无法实现或用户明确要求，否则使用项目实际 Godot 可执行文件及 `--headless` 运行测试、脚本、场景启动和回归检查；改用非 Headless 时在日志中记录原因、替代验证方式和剩余风险。
- 按文档维护规则将该 `AGENTS.md` 版本从 `v1.1.0-260807-214301` 更新为 `v1.2.0-260809-235234`；新增引擎专用默认规则属于向后兼容的能力扩充，因此递增 minor 版本。

## 完成工作

- 更新 `game-agent-template/AGENTS.md`：新增「13. Godot 引擎项目专门要求」及 Headless 测试默认规则。
- 新增本执行日志。

## 更新的需求 ID

- 无；未修改 `REQUIREMENTS.md`。

## 更新的 README 或 DESIGN 章节

- README：无
- DESIGN：无

## 验证方式

- `git fetch --prune`
- `git rev-parse --abbrev-ref --symbolic-full-name '@{u}'`
- `git rev-list --left-right --count HEAD...@{u}`
- `git diff --check`
- 使用 `rg` 检查版本号、Godot 专用章节、`Godot Headless`、`--headless` 与非 Headless 例外均已写入。

## 备注

- 本次为纯文档变更，不启动 Godot 测试；规则本身要求后续 Godot 项目默认采用 Headless 测试。
- 未执行提交、推送或其他发布动作。
