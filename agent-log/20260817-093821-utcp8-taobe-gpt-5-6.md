# 20260817-093821-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

目录提升到 2 级标题
文档缺失时的创建规则应该提升到 2 级标题，应该是叫做仓库规范化。
Agent template 更新规则主要关注的是 template 文件本身的更新规则，这一块应该是新的。
代码清洁规则也应该是新的。
你应该把原来的那份 agents 文档里面的内容整理出多个二级标题，而不是参考我发你的这两个就行了。

## 启动运行时的分支和版本

1. 分支：main。
2. 版本：62125fc。
3. 同步：已执行 git fetch --prune；upstream 为 origin/main，ahead / behind 为 0 / 0，未执行 merge。

## 任务执行时间

1. 开始：2026-08-17 09:25:30 CST。
2. 结束：2026-08-17 09:38:21 CST。
3. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 AGENTS.md。
2. 两份当前子模板 AGENTS.md。
3. 两份模板在 HEAD 中的原始 AGENTS.md，用于恢复完整标准内容分组。
4. 顶层 REQUIREMENTS.md 及已有执行日志。

## 对话与行动记录

1. 将原标准内容从单一 Agent Template 更新规则拆分为多个二级标题。
2. 新增独立的代码清洁规则，并吸收原工程默认规则中的代码、验证、提交和注释约束。
3. 将目录与文件命名提升为 1.2 二级标题。
4. 将文档缺失、模板清理、Game LFS 和 .gitignore 初始化内容归入 1.3 仓库规范化。
5. 新增 1.4 Agent Template 更新规则，专门约束模板文件、元数据、版本、同步和模板演进。
6. 将任务开始、任务执行、REQUIREMENTS、README、DESIGN、父子文档、Worktree、日志拆分和版本管理分别提升为 1.5 至 1.13 二级标题。
7. 更新交叉引用到新的二级标题编号，保留两份模板的通用规则一致，并保留 Game 的 Godot Headless 规则。
8. 同步修正顶层 AGENTS.md 对标准章节范围和复制清理章节的旧引用，并更新顶层版本号。

## 完成工作

1. 更新 app-agent-template/AGENTS.md，版本更新为 v1.3.0-260817-093528。
2. 更新 game-agent-template/AGENTS.md，版本更新为 v1.4.0-260817-093528。
3. 更新顶层 AGENTS.md，版本更新为 v1.0.1-260817-093644。
4. 新增并补全本次执行日志。

## 更新的需求 ID

1. 无新增稳定需求 ID；本次属于顶层 meta-DOC-A-001 元模板结构稳定需求的章节重组。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. 确认两份文档均只有 1、2、3 三个一级分区。
2. 确认两份文档各有 23 个二级标题，且不存在实际三级标题语法。
3. 确认每个二级标题下的非代码内容均为有序列表，嵌套清单按 TAB 缩进。
4. 确认两份文档的 1. 通用规则内容完全一致。
5. 确认旧的 1.1.1、1.2.x 交叉引用已更新为新的二级标题编号。
6. 确认四项元数据、GitHub source 链接和 Game Godot Headless 条目仍然存在。
7. git diff --check 通过。
8. 确认顶层 AGENTS.md 不再引用旧的 §0 或 0–10 节标准内容范围。

## 备注

1. 本次修改两份子模板 AGENTS.md、顶层 AGENTS.md 和顶层执行日志，未修改 README、REQUIREMENTS、DESIGN 或 universe 子模板；保留了工作区中此前已有的其他未跟踪日志。
2. 未执行 App、Game、Godot 或 UE 运行时测试，本次交付物是文档结构整理。
