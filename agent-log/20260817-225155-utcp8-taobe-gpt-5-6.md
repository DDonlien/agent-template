# 20260817-225155-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

代码清洁规则里很多东西应该在 git 规则/perforce 规则里，代码清洁是关于“如何让 ai 清洁工程代码来简化代码内容和结构，避免长期高度复杂、冗余技术债”的；详见附件。

附件中的参考原则包括：不要假设并主动暴露困惑和取舍；只写解决当前问题的最小代码；只修改必须修改的地方并只清理自己产生的问题；定义成功标准并持续验证；信任内部代码和框架保证；只在系统边界校验；不要为不可能发生的场景增加防御性代码；不要吞掉错误；不要为一次性操作创建抽象；优先快速失败。

## 执行信息

1. 执行本次任务的模型：`gpt-5-6`。
2. 启动运行时的分支和版本：`main` / `62125fc`。
3. Git 同步预检：已执行 `git fetch --prune`；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。
4. 任务开始时间：2026-08-17 22:47:00 CST。
5. 任务结束时间：2026-08-17 22:51:55 CST。
6. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 `AGENTS.md` 及其对元模板、子模板同步和日志的要求。
2. `app-agent-template/AGENTS.md` 与 `game-agent-template/AGENTS.md` 的当前未提交版本。
3. 顶层 `REQUIREMENTS.md` 中的 `meta-DOC-A-001`。
4. 当前工作区状态、已有未提交改动、已有执行日志和远端同步状态。
5. 用户提供的附件图像，作为代码清洁原则的参考内容；附件不是独立的仓库修改范围。

## 对话与行动记录

1. 区分用户请求与附件参考：用户请求是重新划分规则职责并重写代码清洁规则；附件提供的是代码简化、反过度工程和防御性编程原则。
2. 保留工作区中此前已经存在的 `AGENTS.md` 修改和多条未跟踪执行日志，没有切换分支、重置、清理或覆盖其他改动。
3. 将两份模板的代码清洁规则改为聚焦代码内容和结构：最小实现、非推测性实现、仅清理当前任务产生的冗余、降低复杂度、保持单一事实来源、边界校验、快速失败、错误可见性、避免一次性抽象、注释意图和分维度验证。
4. 将工作区保护、按需求边界提交、提交前检查、文档/参数/行为拆分提交以及 commit/push 授权纪律移入 Git 规则。
5. 将 Perforce 文件保护、单任务创建或复用一个 pending change、description、opened 检查和 submit 授权纪律集中在 Perforce 规则。
6. 保持两份模板的通用规则内容一致，并递增模板版本：App 为 `v1.6.0-260817-225155`，Game 为 `v1.7.0-260817-225155`。

## 完成工作

1. 更新 `app-agent-template/AGENTS.md` 的代码清洁、Perforce、Git 规则。
2. 更新 `game-agent-template/AGENTS.md` 的代码清洁、Perforce、Git 规则。
3. 新增本次执行日志；未修改 `README.md`、`REQUIREMENTS.md`、`DESIGN.md` 或 `universe` 子模板。

## 更新的需求 ID

1. 无新增稳定需求 ID；本次归入已有的 `meta-DOC-A-001` 元模板结构稳定维护范围。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. `git fetch --prune` 成功，当前分支相对 upstream 为 `0 / 0`。
2. `git diff --check` 通过。
3. 两份模板的 YAML 属性均只保留 `target-repo-type`、`language`、`version`、`source`，版本号和 canonical GitHub source 正确。
4. 两份模板从“通用规则”到“技术栈专用规则”前的内容逐字一致。
5. 代码清洁章节中不再出现 `commit`、`push`、`submit`、`worktree`、Perforce change 或用户改动保护等仓库状态管理条目。
6. Perforce 和 Git 章节分别包含对应的 change、工作区、提交和授权纪律；现有章节结构、技术栈规则和项目专用占位内容保持不变。

## 备注

1. 本次只完成规则文档维护，不涉及应用、游戏、Godot、Unreal 或 Perforce 实际工程操作，因此未执行运行时测试。
2. 未执行 commit、push 或 Perforce submit；交付时工作区仍保留此前已有的未提交改动和历史执行日志。
