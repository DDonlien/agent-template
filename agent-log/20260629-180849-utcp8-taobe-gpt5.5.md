# 20260629-180849-utcp8-taobe-gpt5.5

## 用户原始 Prompt

```text
我希望在 Agents 中再增加这个段落作为命名规则，帮我看一下加在哪里比较好？
## 目录命名规则

本项目的顶层目录应优先按照“交付物 / 职责”命名，而不是按照技术栈命名。目录名应该先回答“这个目录是什么用途”，而不是“它使用什么语言或框架”。例如，Swift 原生 iOS 应用应命名为 `ios/`，macOS 原生应用应命名为 `macos/`；`web/` 仅用于真正的 Web App，并与 `ios/`、`macos/` 等客户端形态并列。官网、宣传页、落地页应命名为 `site/`，管理后台应命名为 `admin/`，不要因为它们使用 TypeScript 就命名为 `ts/` 或 `ts-web/`。如果某个交付物内部同时包含前端和后端，应在该目录下继续做二级拆分，例如 `site/frontend/` 与 `site/backend/`，而不是在顶层直接按技术栈或服务类型拆散。只有当目录职责本身无法清楚表达，或者同一职责下存在多个技术实现时，才允许在名称中补充技术栈信息。目录名应保持小写，使用连字符 `-` 分隔单词，避免重复项目名，也避免使用 `project` 这类信息量较低的泛称。
我没有做任何的换行和缩进，这个需要你帮我处理
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`c150250f9db7bb63794d2d0b826e534e7f6f17b4`
- 同步：`git pull` 后提示 `Already up to date.`

## 任务执行时间

- 开始：`2026-06-29 18:08:04 UTC+8`
- 结束：`2026-06-29 18:08:49 UTC+8`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260629-111435-utcp8-taobe-gpt5.5.md`

## 匹配到的需求

- `[0.3.0-QA-A-000]` 保持 AI 协作可追踪
- `[0.3.0-QA-B-000]` 满足任务完成定义

## 对话与行动记录

- 用户希望把目录命名规则加入 `AGENTS.md`，并要求整理原始段落的换行与缩进。
- Agent 按规范确认分支、版本并执行 `git pull`。
- Agent 阅读相关文档和最新日志。
- Agent 判断该规则属于通用工程目录组织约束，适合放在 `工程默认规则` 之后、`Worktree 工作目录模式` 之前。
- Agent 将原始长段落整理为 `### 8. 目录命名规则`，并将原 `Worktree 工作目录模式` 顺延为 `### 9. Worktree 工作目录模式`。

## 完成工作

- 更新 `AGENTS.md`：新增目录命名规则，明确顶层目录优先按交付物 / 职责命名，补充 `ios/`、`macos/`、`web/`、`site/`、`admin/` 等示例和二级拆分规则。
- 新增本次执行日志。

## 更新的需求 ID

- 无新增需求 ID；本次为协作规范模板补充。

## 更新的 README 或 DESIGN 章节

- README：无
- DESIGN：无

## 验证方式

- `git pull`
- `git diff -- AGENTS.md agent-log/20260629-180849-utcp8-taobe-gpt5.5.md`
- `git status --short`

## 备注

- 本次为文档规则调整，未运行自动化测试。
