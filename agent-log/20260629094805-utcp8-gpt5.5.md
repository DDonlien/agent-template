# 20260629094805-utcp8-gpt5.5

## 用户原始 Prompt

````text
现在[@REQUIREMENTS.md](file:///Users/taobe/Projects/GitHub/Personal/agent-template/REQUIREMENTS.md) 似乎并不遵守agents.md的最新规则，调整一下；然后在这个基础上，三级标题“模块示例”应该改成feature name，对应到worktree（branch，每个worktree应该对应一个branch，这个规则也纳入agents）
````

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`e02cbb171f765a57b29b8a979b3c04ebeccef987`
- 同步：`git pull` 后提示 `Already up to date.`

## 任务执行时间

- 开始：`2026-06-29 09:45:41 UTC+8`
- 结束：`2026-06-29 09:48:05 UTC+8`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `DESIGN.md`
- `agent-log/20260629094241-utcp8-gpt5.5.md`
- 用户附带的最新 `REQUIREMENTS.md` 内容

## 匹配到的需求

- `[0.3.0-QA-A-000]` 保持 AI 协作可追踪
- `[0.3.0-QA-B-000]` 满足任务完成定义

## 对话与行动记录

- 用户指出 `REQUIREMENTS.md` 未遵守 `AGENTS.md` 最新规则，并要求将三级标题从“模块示例”改为 feature name，同时补充 worktree 与 branch 对应规则。
- Agent 执行 `git pull`，确认当前分支和版本。
- Agent 阅读 `AGENTS.md`、`README.md`、`DESIGN.md` 和最新执行日志。
- Agent 更新 `AGENTS.md`：明确 `REQUIREMENTS.md` 通过 Phase、任务区块和 feature name 拆分；三级标题形如 `DOC-A：feature name`，其中 feature name 对应 worktree/branch 的功能名称或工作主题；每个 worktree 应对应一个明确 Git branch。
- Agent 重写 `REQUIREMENTS.md` 模板，使其包含 `# 任务清单`、符合 `Phase - vX.Y.Z - xxx` 的二级标题、符合 `TYPE-A：feature name` 的三级标题，以及带版本前缀的稳定 task ID。
- Agent 使用搜索检查旧的“模块示例/阶段示例”和旧 ID 模式已移除。

## 完成工作

- 更新 `AGENTS.md`：补充 feature name 与 worktree/branch 关系、worktree 与 branch 一一对应规则，并统一 Phase/任务区块/feature name 表述。
- 更新 `REQUIREMENTS.md`：调整为遵守最新 `AGENTS.md` 规则的需求模板。
- 新增本次执行日志。

## 更新的需求 ID

- 新模板中持续约束 ID 更新为 `[0.3.0-QA-A-000]`、`[0.3.0-QA-B-000]`。

## 更新的 README 或 DESIGN 章节

- README：无
- DESIGN：无

## 验证方式

- `git pull`
- 搜索检查：`模块示例|阶段示例|\[DOC-A-|\[FOUND-A-|\[MVP-A-|\[X-A-|\[X-B-|\[B-A-` 在 `REQUIREMENTS.md` 中无匹配。
- `git diff` / `git status` 检查最终变更范围。

## 备注

- 本次为模板文档与协作规范调整，未运行自动化测试。
