# 20260817-092513-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

元数据里只保留这几个信息：
target-repo-type: app/game
language:
version:
source: 这里放这份文件的 github link

## 启动运行时的分支和版本

1. 分支：main。
2. 版本：62125fc。
3. 同步：已执行 git fetch --prune；upstream 为 origin/main，ahead / behind 为 0 / 0，未执行 merge。

## 任务执行时间

1. 开始：2026-08-17 09:23:58 CST。
2. 结束：2026-08-17 09:25:13 CST。
3. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 AGENTS.md。
2. app-agent-template/AGENTS.md 与 game-agent-template/AGENTS.md。
3. 当前工作区状态、远端 GitHub 地址和最近的顶层执行日志。

## 对话与行动记录

1. 用户要求两份模板的 Obsidian 元数据只保留 target-repo-type、language、version 和 source。
2. 从 origin 远程地址确认 GitHub 仓库为 DDonlien/agent-template。
3. 将 source 分别设置为两份 AGENTS.md 在 main 分支上的 GitHub 文件链接。
4. 删除 template_id、template_type、document_type、title 和 scope 等其他元数据。
5. 按文档元数据变更递增 patch 版本，并刷新版本时间戳。

## 完成工作

1. 更新 app-agent-template/AGENTS.md 元数据。
2. 更新 game-agent-template/AGENTS.md 元数据。
3. 新增本次执行日志。

## 更新的需求 ID

1. 无新增稳定需求 ID；本次属于顶层 meta-DOC-A-001 元模板结构稳定需求的元数据格式修正。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. 解析两份 YAML 属性块，确认仅包含四个指定键。
2. 确认 app/game、语言、版本和 GitHub source 值分别正确。
3. git diff --check 通过。
4. 首次校验脚本的期望值空格断言有误，修正断言后重新执行并通过；文件内容没有因此改变。

## 备注

1. 本次只修改两份子模板 AGENTS.md 和顶层执行日志，未修改正文规则。
2. 未执行 App、Game、Godot 或 UE 运行时测试，本次交付物是文档元数据调整。
