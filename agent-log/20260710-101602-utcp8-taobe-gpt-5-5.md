# 20260710-101602-utcp8-taobe-gpt-5-5

## 用户原始 Prompt

```text
根据我们的探讨结果，修改
- app-agent-template/AGENTS.md
- game-agent-template/AGENTS.md
这两份文件中的推荐文件夹结构

参考：ChatGPT-Gitea项目管理建议 (1).md
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`9d64906800f984a0e68b70df8279d8f7c9d98967`
- 同步：第一次 `git fetch --prune` 因网络超时失败；第二次成功完成；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。

## 任务执行时间

- 开始：`2026-07-10 10:16:02 UTC+8`
- 结束：`2026-07-10 10:16:02 UTC+8`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `agent-template/AGENTS.md`
- `agent-template/app-agent-template/AGENTS.md`
- `agent-template/game-agent-template/AGENTS.md`
- `Downloads/ChatGPT-Gitea项目管理建议 (1).md`

## 匹配到的需求

- 无明确需求 ID；本次为子模板协作规范调整。

## 对话与行动记录

- 用户要求根据 Gitea 项目管理建议的讨论结果，修改 `app-agent-template/AGENTS.md` 和 `game-agent-template/AGENTS.md` 中的推荐文件夹结构。
- Agent 按规则执行 Git 同步预检，第一次超时后重试成功。
- Agent 阅读两份子模板 AGENTS 的 Worktree 章节和目录索引章节。
- Agent 将 Worktree 推荐结构从“`<project-name>/` 根目录即 main，其他分支放在 `worktree/<name>/`”改为“`<project-name>/` 是项目容器目录，包含 `main/` 和其他平级 branch worktree”。
- Agent 同步更新两份文件的“目录索引”章节，移除旧的 `worktree/<name>/` 描述，补充 `<project-name>/`、`main/`、`<branch-worktree-name>/`、`_builds/` 的说明。
- Agent 修正 app 模板示例中误含的游戏引擎目录，保持 app 与 game 示例各自的侧重点。

## 完成工作

- 更新 `app-agent-template/AGENTS.md`：§9 Worktree 工作目录模式 + §项目专用内容/目录索引。
- 更新 `game-agent-template/AGENTS.md`：§9 Worktree 工作目录模式 + §项目专用内容/目录索引。
- 新增本次执行日志。

## 更新的需求 ID

- 无。

## 更新的 README 或 DESIGN 章节

- README：无
- DESIGN：无

## 验证方式

- `git fetch --prune`（重试后成功）
- `git rev-parse --abbrev-ref --symbolic-full-name @{u}`
- `git rev-list --left-right --count HEAD...@{u}`
- `rg "worktree/<name>|项目容器目录|`<project-name>/`：项目容器目录" app-agent-template/AGENTS.md game-agent-template/AGENTS.md`
- `git diff -- app-agent-template/AGENTS.md game-agent-template/AGENTS.md`
- `git status --short`

## 备注

- 本次仅修改文档，没有实际创建、移动或删除任何 worktree。
- 顶层元模板 `agent-template/AGENTS.md` 中的 Worktree 章节未在本次修改范围内；如需要同步，可后续单独处理。
