# 20260629-203914-utcp8-taobe-gpt-5-5

## 用户原始 Prompt

```text
main 也创建一个独立 worktree，不要让 main 直接占据根目录。

也就是说，推荐：

<project-name>/
  .repo/                 ← Git 管理仓库，不直接开发
  worktrees/
    main/                ← main 分支的实际工作区
      ios/
      web/
      site/
      godot/
      docs/
    feature-ui/
    agent-godot/
    agent-site/

  我觉得现在的agents.Md没有考虑到这点
```

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`c150250f9db7bb63794d2d0b826e534e7f6f17b4`
- 同步：执行 Git 同步预检；第一次 `git fetch --prune` 超时无输出，第二次完成；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。

## 任务执行时间

- 开始：`2026-06-29 20:39:14 UTC+8`
- 结束：`2026-06-29 20:39:14 UTC+8`
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260629-190443-utcp8-taobe-gpt-5-5.md`

## 匹配到的需求

- `[0.3.0-QA-A-000]` 保持 AI 协作可追踪
- `[0.3.0-QA-B-000]` 满足任务完成定义

## 对话与行动记录

- 用户指出当前 `AGENTS.md` 的 worktree 结构没有明确要求 `main` 分支也使用独立 worktree，不应让 `main` 直接占据项目根目录。
- Agent 按规则执行 Git 同步预检，确认分支与 upstream 状态可继续。
- Agent 阅读相关文档和最新日志。
- Agent 更新 `AGENTS.md` 第 9 节 Worktree 工作目录模式：将推荐结构改为项目容器目录下包含 `.repo/` 和 `worktrees/`，其中 `worktrees/main/` 是 main 分支实际工作区。
- Agent 补充说明：`<project-name>/` 只是容器目录，不直接作为任何 branch 的开发工作区；`.repo/` 只用于 Git 管理入口；`main` 是默认主分支的特殊稳定分支名，对应 `worktrees/main/`。
- Agent 调整示例说明，避免把 `main` 与 `feature-ui` 等普通示例名混为一谈。

## 完成工作

- 更新 `AGENTS.md` Worktree 工作目录模式示例，明确 `worktrees/main/` 是 main 分支实际工作区。
- 补充项目容器目录、`.repo/`、`worktrees/main/` 的职责边界。
- 新增本次执行日志。

## 更新的需求 ID

- 无新增需求 ID；本次为协作规范模板调整。

## 更新的 README 或 DESIGN 章节

- README：无
- DESIGN：无

## 验证方式

- `git fetch --prune`
- `git rev-parse --abbrev-ref --symbolic-full-name @{u}`
- `git rev-list --left-right --count HEAD...@{u}`
- `rg "main 分支也应使用独立 worktree|worktrees/main|main` 仅用于表示|项目容器目录不直接开发" AGENTS.md`
- `git diff -- AGENTS.md agent-log/20260629-203914-utcp8-taobe-gpt-5-5.md`
- `git status --short`

## 备注

- 本次仅修改文档，没有实际创建、移动或删除任何 worktree。
