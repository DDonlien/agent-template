# 20260817-084914-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

把这 2 份 template 结构拆成

属性 //兼容 obsidian 的属性结构

# 1. 通用规则
## 1.x. 代码清洁规则
## 1.x. Agent Template 更新规则

# 2. 技术栈专用规则
## 2.x. Godot 规则
## 2.x. UE 规则
## 2.x. Typscript 规则

# 3. 项目专用规则

x.x.里的内容你帮我按实际情况整理和分配顺序，我打的只是示例
每一个级别TAB缩进一次

## 启动运行时的分支和版本

- 分支：main
- 版本：62125fc
- 同步：已执行 git fetch --prune；upstream 为 origin/main，ahead / behind 为 0 / 0，未执行 merge。

## 任务执行时间

- 开始：2026-08-17 08:38:13 CST
- 结束：2026-08-17 08:49:14 CST
- 任务结束时是否执行了提交：否

## 已阅读上下文

- 顶层 AGENTS.md。
- app-agent-template/AGENTS.md 与 game-agent-template/AGENTS.md。
- 两个模板的 README.md、REQUIREMENTS.md，以及最近的顶层 agent-log。
- 当前工作区状态和远端同步状态。

## 匹配到的需求

- 无新增稳定需求 ID；本次是用户明确要求的模板协作规范结构重构，归入顶层 meta-DOC-A-001 的子模板可用性维护范围。

## 对话与行动记录

- 将两份模板统一设计为 Obsidian YAML 属性块、1. 通用规则、2. 技术栈专用规则和 3. 项目专用规则三层结构。
- 将原标准内容 0–11 重排为 1.1 代码清洁规则和 1.2 Agent Template 更新规则，并把原有文档缺失、任务开始/执行、需求、README、DESIGN、父子目录、Worktree、日志和版本管理内容保留在 1.2 的子章节中。
- 将 Game 原有 Godot Headless 和 Unreal Engine 规则分别放入 2.1 和 2.2；两份模板都增加适用范围明确的 2.3 TypeScript 规则。
- 将项目概况、技术栈与命令、文档入口、目录索引、子功能入口和 App/Game 补充约束统一放入 3.x。
- 将嵌套 Markdown 清单的缩进统一为每层一个 TAB；标题保持顶格，避免前置 TAB 使 Markdown/Obsidian 将标题解析为代码块。
- 将 Game 模板中的 universe 链接改为可复制、可移植的相对链接，并修正重排后失效的章节引用。
- 按向后兼容的结构扩充递增版本号：App 从 v1.1.0 更新到 v1.2.0，Game 从 v1.2.0 更新到 v1.3.0，时间戳统一为 260817-084914。

## 完成工作

- 重排 app-agent-template/AGENTS.md。
- 重排 game-agent-template/AGENTS.md。
- 新增本次执行日志。

## 更新的需求 ID

- 无；未修改 REQUIREMENTS.md。

## 更新的 README 或 DESIGN 章节

- README：无。
- DESIGN：无。

## 验证方式

- git fetch --prune。
- 检查分支、upstream、ahead / behind 和工作区状态。
- 验证两个文件均包含完整 YAML 属性块，且属性包含 template_id、template_type、document_type、title、language、version 和 scope。
- 验证每个文件有且只有 1、2、3 三个顶层章节，并包含对应的 1.x、2.x、3.x 二级章节。
- 验证两份文档的 1. 通用规则内容一致。
- 使用 rg 检查残留的绝对 file 链接、旧章节引用和未转换的空格缩进清单。
- git diff --check 通过。

## 备注

- 本次只改动两份子模板 AGENTS.md 和顶层执行日志；未修改顶层 AGENTS.md、README.md、REQUIREMENTS.md、DESIGN.md 或 universe 子模板。
- 标题没有使用 TAB 前置缩进，因为这会破坏 Markdown/Obsidian 标题解析；TAB 已用于每一层嵌套清单。
- 未执行 App、Game、Godot 或 UE 运行时测试，本次交付物是文档结构重排。
