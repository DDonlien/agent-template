# 20260818-011552-utcp8-taobe-gpt-5-6

## 用户原始 Prompt

添加以下：
1、2、3、4、5、6，但说人话

## 执行信息

1. 执行本次任务的模型：`gpt-5-6`。
2. 启动运行时的分支和版本：`main` / `62125fc`。
3. Git 同步预检：已执行 `git fetch --prune`；upstream 为 `origin/main`，ahead / behind 为 `0 / 0`，未执行 merge。
4. 任务开始时间：2026-08-18 01:13:51 CST。
5. 任务结束时间：2026-08-18 01:15:52 CST。
6. 任务结束时是否执行了提交：否。

## 已阅读上下文

1. 顶层 `AGENTS.md` 及其对两份子模板同步、版本号和执行日志的要求。
2. `app-agent-template/AGENTS.md` 与 `game-agent-template/AGENTS.md` 的当前未提交版本。
3. 用户上一条消息确认的外部规则候选清单；本次的 1—6 指该清单中“通用规则：记录与事实管理”的前六项。
4. 比本对话上一条日志更新的 `20260818-011145` 日志；保留其中刚完成的 Agent Template 更新规则措辞及其他已有改动。

## 对话与行动记录

1. 用户要求把已确认的前六项通用规则加入模板，并改写成自然、直接的中文。
2. 在不改写既有条目的前提下，将六项内容分别加入任务开始、事实来源、任务执行、父子文档和构建输出相关章节。
3. 将相同内容同步写入 App 与 Game 模板；未把候选规则的外部来源说明写进模板。

## 完成工作

1. 新增持续更新文档与中断后可接手的规则。
2. 新增区分实现、验证、提交、推送、发布以及警告、失败的规则。
3. 新增事实来源优先级、资料冲突和事实／推断／待验证内容的处理规则。
4. 新增阅读并同步更新架构与 Schema 文档的规则。
5. 新增仅在确实需要独立维护时创建子功能文档的规则，并排除输出目录、临时目录和平行需求文档。
6. 新增默认不额外打包、需要输出时从权威源重新生成的规则。
7. 按规则递增版本号：App 为 `v1.8.0-260818-011552`，Game 为 `v1.9.0-260818-011552`。

## 更新的需求 ID

1. 本次归入已有 `meta-DOC-A-001` 元模板结构稳定维护范围；未新增稳定需求 ID。

## 更新的 README 或 DESIGN 章节

1. README：无。
2. DESIGN：无。

## 验证方式

1. `git diff --check` 通过。
2. App 与 Game 从 `# 通用规则` 到 `# 技术栈专用规则` 的内容逐字一致。
3. 六项新增内容均在两份模板中各出现一次。
4. App 模板不包含 Godot 或 Unreal Engine 规则；Game 模板仍保留两节引擎规则。
5. 未执行 commit、push、PR 或 Perforce submit。

## 备注

1. 本次只新增用户确认的六项规则、更新两份模板版本号并创建执行日志；没有改写或删除已有规则。
